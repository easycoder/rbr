!   uilocal.ecs - the main script for the user interface

    script UILocal

    dictionary Map
    dictionary Request
    module UI

    ! Launch the UI
    run `uicore.ecs` as UI with Map and Request
    release parent
    on message
    begin
        if the message is `map` send Map to the sender
    end
    stop
