# RBR test config — dropped into /etc/mosquitto/conf.d/ during tests
# Adds websocket listener for the UI and allows anonymous access

listener 1883
allow_anonymous true

listener 9001
protocol websockets
allow_anonymous true
