'''
Write a pyside button widget BasicKeyboardButton with parent PushButton.
Functions:
__init__(self, width, height, text, icon=None, uppercase=False, onClick=None):
    def __init__(self, width, height, text):
setText(self, text):
setIcon(self, icon):
onClick(self, cb):
When the button is clicked, call function 'cb' with the value of 'text'.
The text to be shown on the button can be any string, or None.
The icon to be shown on the button can be any graphic, or None
The button has rounded corders of radius 20% of the button height
The font size is half the button height
The background of the button is white
Apply a light shadow to the right and bottom of the button
When the button is clicked it moves 2 pixels down and right,
waits 200ms then move back to its original position.

Write a widget DefaultKeyboardButton(BasicKeyboardButton).
Functions:
__init__(self, width, text, None, uppercase, onClick):
    super.__init__(height, height, text, None, uppercase, onClick)
onClick(self, cb):
When the button is clicked, call the function 'cb' with the value of 'text'.

Write a widget SpecialKeyboardButton(BasicKeyboardButton).
Functions:
__init__(self, width, height, icon, text, onClick):
    super.__init__(width, height, None, icon)
onClick(self, cb): Overrides the parent function
When the button is clicked, call the function 'cb' with the value of 'text'.

Write a KeyboardRow, possibly as a subclass of QHBoxLayout.
This takes a list of widgets, which it places
one after another horizontally.
Functions:
__init__(self, list):
where 'list' is a list of the widgets to be added.

Write a KeyboardView, possibly as a subclass of QVBoxLayout.
Functions:
__init__(self, list):
where 'list' is a list of KeyboardRow to be added.

Write a Keyboard
Functions:
__init__(self, list):
where 'list' is a list of KeyboardView.
The function displays the first KeyboardView, the rest are hidden.
selectView(self, v = 0)
If v is not the current view, hide the current view then show the one specified.

Write a test class KeyboardTest():
Set a view index variable to 0
Functions:
onClickChar(self, keycode): Print the key code to the console
onClickShift(self): Print 'Shift' to the console. If the view index is 0,
hide view 0, show view 1 and set the view index to 1. If the view index is 0,
hide view 1, show view 0and set the view index to 0. 
onClickBack(self): Print 'Back' to the console

Create a window 600 pixels wide and 350 high with background #ccc.
Compute a standard height value, to be 15% of the window height. 

Layout 0

Create a row list - an empty list of KeyboardRow

Create a list of DefaultKeyboardButton with the text
        '1', '2', '3', '4', '5', '6', '7', '8', '9', '0'
The 'callback' function is onClickChar
Create a KeyboardRow with this list
Add the KeyboardRow to the KeyboardView

Create a list of DefaultKeyboardButton with the lowercase text
        'q', 'w', 'e', 'r', 't', 'y', 'u', 'i', 'o', 'p'
The 'callback' function is onClickChar
Create a KeyboardRow with this list
Add the KeyboardRow to the row list

Create an empty list of widgets
Add a horizontal stretch to the widget list
Create a list of DefaultKeyboardButton with the lowercase text
        'a', 's', 'd', 'f', 'g', 'h', 'j', 'k', 'l'
The 'callback' function is onClickChar
Append this list to the widget list
Add a horizontal stretch to the widget list
Create a KeyboardRow with the widget list
Add the KeyboardRow to the row list

Create an empty list of widgets
Create a SpecialKeyboardButton with
    a width 150% of the standard height
    a standard height
    an icon taken from the graphic 'up.png'
    the 'callback' function is onClickShift
Append the SpecialKeyboardButton to the widgt list
Append a spacer whose width is 5% of the standard height to the widget list
Create a list of DefaultKeyboardButton with the lowercase text
        'z', 'x', 'c', 'v', 'b', 'n', 'm'
The 'callback' function is onClickChar
Append this list to the widget list
Append a spacer whose width is 5% of the standard height to the widget list
Create a SpecialKeyboardButton with
    a width 150% of the standard height
    a standard height
    an icon taken from the graphic 'back.png'
    the 'callback' function is onClickBack
Append the SpecialKeyboardButton to the widget list
Create a KeyboardRow with the widget list
Add the KeyboardRow to the row list

create an empty view list - a list of KeyboardView
Create a KeyboardView with the row list
Append the KeyboardView to the view list

Layout 1

Create a row list - an empty list of KeyboardRow

Create a list of DefaultKeyboardButton with the text
        '1', '2', '3', '4', '5', '6', '7', '8', '9', '0'
The 'callback' function is onClickChar
Create a KeyboardRow with this list
Add the KeyboardRow to the KeyboardView

Create a list of DefaultKeyboardButton with the uppercase text
        'Q', 'W', 'E', 'R', 'T', 'Y', 'U', 'I', 'O', 'P'
The 'callback' function is onClickChar
Create a KeyboardRow with this list
Add the KeyboardRow to the row list

Create an empty list of widgets
Add a horizontal stretch to the widget list
Create a list of DefaultKeyboardButton with the vv text
        'A', 'S', 'D', 'F', 'G', 'H', 'J', 'K', 'L'
The 'callback' function is onClickChar
Append this list to the widget list
Add a horizontal stretch to the widget list
Create a KeyboardRow with the widget list
Add the KeyboardRow to the row list

Create an empty list of widgets
Create a SpecialKeyboardButton with
    a width 150% of the standard height
    a standard height
    an icon taken from the graphic 'up.png'
    the 'callback' function is onClickShift
Append the SpecialKeyboardButton to the widgt list
Append a spacer whose width is 5% of the standard height to the widget list
Create a list of DefaultKeyboardButton with the uppercase text
        'Z', 'X', 'C', 'V', 'B', 'N', 'M'
The 'callback' function is onClickChar
Append this list to the widget list
Append a spacer whose width is 5% of the standard height to the widget list
Create a SpecialKeyboardButton with
    a width 150% of the standard height
    a standard height
    an icon taken from the graphic 'back.png'
    the 'callback' function is onClickBack
Append the SpecialKeyboardButton to the widget list
Create a KeyboardRow with the widget list
Add the KeyboardRow to the row list

Create a KeyboardView with the row list
Append the KeyboardView to the view list

Create a Keyboard with the view list

Add the Keyboard to the window
Show the window

Note: QGraphicsDropShadowEffect is part of QtWidgets, not QtCore
'''
