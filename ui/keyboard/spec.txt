'''
Write pyside code for a square button.
The __init__ function should accept the width and height of the button and the text to display
The button should have rounded corders of radius 20% of the button height
Set the font size to half the button height
The background of the button is white
Apply a light shadow to the right and bottom of the button
When the button is clicked it moves 2 pixels down and right,
waits 200ms then move back to its original positionOther functions:
setWidth(self, width) sets the width
setText(self, text) sets the text
setCallback(self, cb) sets a function to call when the button is clicked.
The callback function is of the form callback(key) where 'key' is the button text

Create a window 600 pixels wide and 300 high

Using the RoundedButton class above, create a row of buttons with the following labels:
1, 2, 3, 4, 5, 6, 7, 8, 9, 0
Create a second row of buttons with the labels:
Q, W, E, R, T, Y, U, I, O, P
Create a third row of buttons with the labels:
A, S, D, F, G, H, J, K,L
Create a fourth row of buttons with the labels:
Z, X, C, V, V, B, N, M
All four rows should be centre aligned horizontally

At the left of row 4, create 2 items:
1 a button 75 pixels wide and 50 pixels high, with the graphic 'up.png'.
2 a 3 pixel spacer

At the right of row 4, create 2 items:
1 a 3 pixel spacer
2 a button 75 pixels wide and 50 pixels high, with the graphic 'back.png'.

Note: QGraphicsDropShadowEffect is part of QtWidgets, not QtCore
'''
